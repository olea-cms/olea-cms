{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:live": "vite",
    "build": "tsc && vite build",
    "dev": "tsc && vite build --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.3.4"
  },
  "dependencies": {
    "vite-plugin-pug-transformer": "^1.0.5"
  }
}
